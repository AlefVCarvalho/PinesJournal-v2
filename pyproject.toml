[project]
name = "pinesjournal-v3"
version = "0.2.0"
description = "Pine's Journal - PWA pessoal com Cloudflare Workers, FastAPI e D1"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.116,<1.0",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
